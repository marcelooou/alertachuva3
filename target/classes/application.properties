# ---------------------------------------------------------
# Application Properties para Spring Boot
# ---------------------------------------------------------

# Porta em que o Spring Boot irá rodar
server.port=8080

# ---------------------------------------------------------
# Configurações do Datasource (Oracle XE)
# ---------------------------------------------------------
spring.datasource.url=jdbc:oracle:thin:@oracle-db:1521/XE
spring.datasource.username=system
spring.datasource.password=oracle
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# ---------------------------------------------------------
# Configurações do JPA / Hibernate
# ---------------------------------------------------------
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ---------------------------------------------------------
# (Opcional) Configurações de Log
# ---------------------------------------------------------
# logging.level.org.springframework=INFO
# logging.level.com.fiap.globalsolution=DEBUG

# ---------------------------------------------------------
# (Opcional) Configurações de Swagger/OpenAPI
# ---------------------------------------------------------
# springdoc.api-docs.path=/api-docs
# springdoc.swagger-ui.path=/swagger-ui.html
